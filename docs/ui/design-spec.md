# UI 设计约定

适用于本项目所有小程序和 H5 Demo 页面的核心视觉约定。

## 色彩

**主色**：各项目自定义（如食物记录用金黄 `#e5a00d`，通用型用蓝色 `#2563eb`）

**语义色（统一）**：

- 成功：`#22c55e`
- 警告：`#f59e0b`
- 危险/错误：`#ef4444`

**文本色**：

- 主文本：`#2a2a2a`
- 次要文本：`#666666`
- 辅助/占位文本：`#999999`

## 间距

以 **4px** 为基础单位：


| 名称  | 值    |
| --- | ---- |
| 紧凑  | 8px  |
| 中等  | 12px |
| 标准  | 16px |
| 宽松  | 24px |
| 超宽  | 32px |


## 字号


| 用途   | 大小   |
| ---- | ---- |
| 辅助说明 | 12px |
| 正文   | 14px |
| 大号正文 | 16px |
| 小标题  | 18px |
| 大标题  | 24px |


## 圆角


| 场景      | 值      |
| ------- | ------ |
| 小标签、小按钮 | 6px    |
| 卡片、输入框  | 10px   |
| 大卡片     | 16px   |
| 胶囊形     | 9999px |


## 组件约定

- **可点击区域**最小高度：44px（满足移动端触控目标）
- **H5 页面**最大宽度：480px，居中显示
- **阴影**：慎用，仅用于浮层、FAB、主要操作按钮

## 财务助理 Demo — 光明通讯终端财务模式

财务助理 Demo 复用光明通讯终端主题，聚焦文字记账与收支查询交互。

### 配色


| 用途      | 色值                      | 说明                    |
| ------- | ----------------------- | --------------------- |
| 页面背景    | `#f8f7f4`               | 温暖米白                  |
| 次级背景    | `#f0efec`               | 略深米白，用于悬停底色           |
| 卡片背景    | `#ffffff`               | 纯白                    |
| 主操作色    | `#e5a00d`               | 金黄，用于按钮、光标、信号条、终端圆点   |
| 主操作浅色   | `rgba(229,160,13,0.12)` | 金黄透明，用于可点击项激活背景       |
| 氛围渲染色   | `#e8a0b0` / `#fff0f0`   | 浅粉，用于右上角光晕            |
| 主文本     | `#2a2a2a`               | 深灰                    |
| 次要文本    | `#666666`               | 中灰                    |
| 辅助/占位文本 | `#999999`               | 浅灰                    |
| 基础边框    | `#eee`                  | 极浅灰                   |
| 危险色     | `#ef4444`               | 红色，用于删除、录音中指示、未配置提示红点 |
| 成功色     | `#22c55e`               | 绿色                    |


### 字体

- **终端标识、面板头、系统提示**：Space Mono（等宽字体）
- **中文正文、金额、标签**：Noto Sans SC

### 页面构成

财务助理共三个页面，均为纵向 flex、`100dvh`、`max-width: 480px` 居中布局：

#### 1. 首页（index.html）


| 区域      | 说明                                                                                     |
| ------- | -------------------------------------------------------------------------------------- |
| 顶部栏     | 信号指示器（三格金黄信号条 + `CONNECTED` 文字）+ 右侧圆形个人资料入口（未配置 API Key 时显示红点）                         |
| 终端头     | 金黄圆点（呼吸光晕动效）+ `FINANCE ASSISTANT` 标识 + 频道号 `FIN-01`                                    |
| 月份选择行   | `MONTH>` 等宽标签 + 原生 `<input type="month">`                                              |
| 月度摘要面板  | 终端面板（`MONTHLY_SUMMARY`），3 列网格：总支出 / 总收入 / 净收支；支出和收入格可点击（点击展开该月详细记录，激活态金黄下边线），净收支格为静态展示 |
| AI 回复面板 | 终端面板（`ASSISTANT_REPLY`），展示逐字打出的助理回复；打字中左侧显示 3px 金黄边线；空态显示 `> 等待输入...`                  |
| 固定底部输入栏 | 全宽渐变遮罩 + 文字输入框 + 金黄发送按钮（纸飞机图标）                                                         |


#### 2. 收支详情页（detail.html）


| 区域        | 说明                                                |
| --------- | ------------------------------------------------- |
| 终端导航栏     | 返回箭头 + 页面标题（`EXPENSE_RECORDS` / `INCOME_RECORDS`） |
| 月份选择行     | 同首页                                               |
| 类型切换 Tabs | 全部 / 支出 / 收入，激活态金黄下划线                             |
| 搜索行       | `SEARCH>` 等宽标签 + 文字过滤输入框                          |
| 月度汇总面板    | 终端面板（`MONTHLY_TOTAL`），显示总金额 + 记录条数                |
| 记录列表面板    | 终端面板（`RECORD_LIST`），逐条展示流水；空态显示 `> 暂无记录`          |


#### 3. 个人资料页（profile.html）

终端面板风格，依次包含：


| 面板标识              | 内容                                     |
| ----------------- | -------------------------------------- |
| `PERSONAL_INFO`   | 称呼输入框                                  |
| `BUDGET_CONFIG`   | 月预算（元）数字输入框                            |
| `API_CREDENTIALS` | 千问 API Key（必填）+ 智谱 API Key（可选），均为密码型输入 |
| `AI_MODEL`        | 模型选择按钮组                                |


底部操作：「保存设置」主按钮 + 「清除聊天记录」危险轮廓按钮。

### 关键视觉效果

- **双色氛围光晕**：右上角浅粉 radial-gradient + 左下角金黄 radial-gradient，`position: fixed` 叠底，不影响交互
- **金黄信号条呼吸动效**（`signalPulse`）：三格信号条交替透明度 1↔0.5，间隔 0.2s
- **终端圆点光晕动效**（`dotGlow`）：金黄 box-shadow 0 0 4px ↔ 8px 循环
- **AI 打字动效**：回复逐字追加，打字期间左侧显示 3px 金黄实线
- **摘要格点击反馈**：hover 金黄淡底，active 金黄下边线 + 轻微缩放（scale 0.96）
- **Toast 提示**：底部弹出，自动消失

### 组件规格


| 组件                                 | 规格                                                                     |
| ---------------------------------- | ---------------------------------------------------------------------- |
| 终端面板（`.terminal-panel`）            | 白色卡片，`border-radius: 12px`，`border: 1px solid #eee`，面板头等宽字体大写、字符间距 1px |
| 输入框（`.chat-input` / `.form-input`） | 圆角 8px，边框 1px `#eee`，focus 态金黄边框 + 金黄淡底色                               |
| 发送按钮（`.send-btn`）                  | 40×40px 圆形，金黄背景，白色图标，active 缩放 0.92                                    |
| 主按钮（`.btn-primary`）                | 全宽，金黄背景，白色文字，圆角 9999px，高度 48px                                         |
| 危险轮廓按钮（`.btn-danger-outline`）      | 全宽，透明背景，红色边框与文字，圆角 9999px                                              |
| 类型 Tab（`.type-tab`）                | 胶囊形，激活态金黄背景白色文字，非激活态透明                                                 |


## 冒险游戏 Demo — 东方水墨武侠风

冒险游戏 Demo 采用东方水墨武侠视觉主题：白色宣纸为底，墨色勾勒，少量朱砂红和金色描边点缀。

### 配色


| 用途       | 色值                         | 说明                      |
| -------- | -------------------------- | ----------------------- |
| 页面背景     | `#f5f0e6`                  | 温暖宣纸白                   |
| 次级背景     | `#ede7d9`                  | 略深宣纸色                   |
| 卡片背景     | `#faf7f0`                  | 微米白                     |
| 主墨色      | `#1a1a1a`                  | 深墨，用于标题和正文              |
| 中墨色      | `#3d3530`                  | 中墨，次级正文                 |
| 淡墨色      | `#6b6156`                  | 淡墨，辅助文字                 |
| 墨洗色      | `#9e9486`                  | 水墨淡彩，占位文字               |
| 朱砂红      | `#b5382b`                  | 点缀色，用于按钮、印章徽标、圆点、选中态    |
| 朱砂红淡底    | `rgba(181, 56, 43, 0.12)`  | 朱砂红透明，用于标签背景            |
| 金色       | `#b8963e`                  | 描边色，用于进度条、focus 边框、装饰线   |
| 金色淡底     | `rgba(184, 150, 62, 0.15)` | 金色透明，用于 active/hover 背景 |
| 边框       | `#d8d0c0`                  | 宣纸灰                     |
| 危险色      | `#b5382b`                  | 同朱砂红                    |


### 字体

- **书法标题**：Ma Shan Zheng / ZCOOL XiaoWei（楷书/手写体）
- **面板标题/副标题**：ZCOOL XiaoWei（站酷小薇体）
- **中文正文**：Noto Serif SC（思源宋体）

### 页面构成

冒险游戏共两个页面，均为纵向 flex、`100dvh`、`max-width: 480px` 居中布局：

#### 1. 首页（index.html）


| 区域     | 说明                                                         |
| ------ | ---------------------------------------------------------- |
| 顶部栏    | 金色信号条（呼吸动效）+ `CONNECTED` 文字 + 右侧设置按钮（未配置 API Key 时显示朱砂红点）  |
| 水墨标题头  | 朱砂红圆点（光晕动效）+ 「江湖奇缘」书法标题 + 「卷一」频道号                          |
| 欢迎卷轴面板 | 宣纸卡片，左侧金色装饰线，「序章」面板头                                       |
| 人物档案面板 | 宣纸卡片，右上角「人物」朱砂印章装饰，显示角色名和风格标签                              |
| 主操作按钮  | 「踏入江湖」朱砂红渐变按钮，金色上下描边                                       |
| 次操作按钮  | 「人物初始化」墨色描边按钮、「续写未竟之旅」                                     |
| 墨迹分隔符  | 居中「卷宗」文字 + 两侧淡化线条                                          |
| 往事录面板  | 宣纸卡片，历史故事列表，每条显示标题、世界观标签（朱砂红淡底）、章节/场景数、日期                  |


#### 2. 游戏页（game.html）


| 区域     | 说明                                                         |
| ------ | ---------------------------------------------------------- |
| 背景图层   | 文生图全屏显示，应用水墨淡化滤镜（`saturate(0.35) contrast(0.85)`）         |
| 背景遮罩   | 宣纸色渐变叠加（顶部透明 → 底部不透明），保证文字可读                               |
| 画境角标   | 右上角朱砂红描边胶囊，「画境生成中...」提示                                    |
| 导航栏    | 返回按钮 + 书法体标题 + 金色章节进度                                      |
| 进度条    | 2px 金色渐变，带金色光晕                                             |
| 使命条    | 金色渐变底，「使命」印章标签 + 目标文字                                      |
| 故事内容区  | 宋体正文，行高 2.1，墨迹入场动画（fadeIn + blur）；历史场景虚线分隔                  |
| 世界观选择  | 宣纸底按钮，左侧金色边线，朱砂红序号印章                                       |
| 自由行动区  | 宣纸色渐变底栏，「灵感」提示标签 + 宋体输入框（gold focus 边框）+ 墨色发送按钮            |
| 结局面板   | 「故事终章」书法标题 + 两侧金色装饰线 + 「归去来兮」返回按钮                          |
| 加载浮层   | 宣纸半透明底，墨色旋转加载 + 「运笔中」书法文字                                  |


### 关键视觉效果

- **宣纸纹理**：CSS `repeating-linear-gradient` 横竖细线叠加，模拟纸纤维
- **水墨晕染光晕**：右上墨色 radial-gradient + 左下朱砂 radial-gradient，`position: fixed` 叠底
- **金色信号条呼吸动效**（`signalPulse`）：透明度 1↔0.4，2s 周期
- **朱砂圆点光晕动效**（`dotGlow`）：box-shadow 4px↔12px，3s 周期
- **墨迹入场动效**（`inkFadeIn`）：`opacity 0→1` + `translateY(8px→0)` + `blur(2px→0)`，0.8s
- **卷轴金色装饰线**：面板左侧 2px 渐变金线（上下淡出）
- **印章风格徽标**：朱砂红边框 + 朱砂淡底小字（如人物档案右上角）
- **背景图水墨滤镜**：`filter: saturate(0.35) contrast(0.85) brightness(1.05)` 降饱和

### 组件规格


| 组件                      | 规格                                                                              |
| ----------------------- | ------------------------------------------------------------------------------- |
| 卷轴面板（`.terminal-panel`） | 宣纸白卡片，`border-radius: 8px`，`border: 1px solid #d8d0c0`，左侧金色装饰线，面板头站酷小薇字体、字间距 2px |
| 输入框（`.form-input`）      | 圆角 6px，边框 1px `#d8d0c0`，focus 态金色边框 + 金色淡底                                      |
| 主按钮（`.btn-primary`）     | 全宽，朱砂红渐变背景，宣纸白文字，圆角 6px，高度 52px，金色上下描边                                         |
| 次按钮（`.btn-secondary`）   | 全宽，透明背景，墨色淡边框，站酷小薇字体，active 态金色淡底                                               |
| 发送按钮（`.action-send`）    | 墨色背景，宣纸白文字，圆角 6px，书法字体                                                          |
| 选项按钮（`.choice-btn`）     | 宣纸白底，细边框，朱砂红印章序号                                                                |
| 故事标签（`.story-card-tag`） | 朱砂红淡底 + 站酷小薇字体                                                                  |


## 扑克教练 demo

需要时，阅读 docs\ui\poker-coach\poker-coach-design.md